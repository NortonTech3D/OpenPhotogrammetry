# tests/unit

Unit tests verify module-level behavior.

Included checks:

- `unit.target_link_smoke`: confirms all public module targets link together
- `unit.<module>_behavior`: behavior checks for each module (`core`, `io`, `camera`, `features`, `sfm`, `mvs`, `meshing`, `texturing`, `geo`)
